name: Publish fgmetric to PyPI

on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'

# Ensure only one publish workflow runs at a time
concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: false

env:
  UV_VERSION: 0.12.0
  PYTHON_VERSION: 3.12

jobs:
  tests:
    name: Static Analysis Checks
    uses: "./.github/workflows/python_package.yml"

  build:
    name: Build Package
    runs-on: ubuntu-latest
    needs: [tests]
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0

      - name: Set up uv and Python
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d  # v10.0.1
        with:
          version: "${{ env.UV_VERSION }}"
          python-version: ${{ env.PYTHON_VERSION }}
          enable-cache: 'true'
          cache-suffix: ${{ env.PYTHON_VERSION }}

      - name: Build sdist and wheel
        run: |
          uv build --sdist --wheel

      - name: List built artifacts
        run: |
          ls -la dist/

      - name: Verify package metadata
        run: |
          uv run --with twine twine check --strict dist/*

      - name: Upload build artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: dist
          path: dist/
          if-no-files-found: error
          retention-days: 30

  verify-install:
    name: Verify Installable Artifact
    needs: [build]
    runs-on: ubuntu-latest

    permissions:
      contents: read

    strategy:
      fail-fast: false
      matrix:
        PYTHON_VERSION: ["3.12", "3.13", "3.14"]
        DIST: [whl, tar.gz]
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: dist
          path: dist/

      - name: Install uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d  # v10.0.1
        with:
          version: "${{ env.UV_VERSION }}"
          # `enable-cache` defaults to `auto`, which is on for GitHub-hosted runners.
          # This job exists to prove the artifact installs from a clean slate, so
          # resolve against upstream rather than a warm cache.
          enable-cache: 'false'

      - name: Import the built distribution
        run: |
          uv run --no-project \
            --python ${{ matrix.PYTHON_VERSION }} \
            --with dist/*.${{ matrix.DIST }} \
            python -P - <<'PY'
          import importlib, pkgutil
          import fgmetric as pkg
          for mod in pkgutil.walk_packages(pkg.__path__, pkg.__name__ + "."):
              importlib.import_module(mod.name)
          print("imported all submodules of", pkg.__name__)
          PY

  publish-pypi:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    needs: [build, verify-install]

    environment:
      name: pypi  # Requires manual approval if configured
      url: https://pypi.org/p/${{ github.event.repository.name }}

    # Required for PyPI Trusted Publishing (OIDC)
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: dist
          path: dist/

      - name: List artifacts to publish
        run: |
          echo "Publishing the following artifacts to PyPI:"
          ls -la dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33  # v1.14.2
        with:
          skip-existing: true
          print-hash: true
          verbose: true

  make-changelog:
    name: Draft Changelog
    runs-on: ubuntu-latest
    needs: [publish-pypi]
    outputs:
      release_body: "${{ steps.git-cliff.outputs.content }}"
    steps:
      - name: Checkout the Repository at the Tagged Commit
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0
          ref: "${{ github.ref_name }}"

      - name: Generate a Changelog
        uses: orhun/git-cliff-action@f50e11560dce63f7c33227798f90b924471a88b5  # v4.8.0
        id: git-cliff
        with:
          config: pyproject.toml
          args: --latest --verbose
        env:
          GITHUB_REPO: "${{ github.repository }}"

  make-github-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    environment: github
    permissions:
      contents: write
    needs: make-changelog
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: dist
          path: dist/

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228  # v3.0.2
        with:
          name: '${{ github.ref_name }}'
          body: '${{ needs.make-changelog.outputs.release_body }}'
          draft: false
          prerelease: false
          files: "dist/*"
